import React from 'react';
import ReactDOM from 'react-dom/client';
import GiphyExpertApp from './GiphyExpertApp';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <GiphyExpertApp />
);
